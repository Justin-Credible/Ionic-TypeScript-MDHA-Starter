<?xml version="1.0" encoding="UTF-8"?>
<plugin xmlns="http://apache.org/cordova/ns/plugins/1.0"
        xmlns:android="http://schemas.android.com/apk/res/android"
        id="net.justin-credible.iosname"
        version="1.0">

  <name>iOS Name Overrider</name>

  <description>
    Specifies a custom name for iOS (since iOS only shows very short names
    on its home screen, while other platforms don't have this limitation).
  </description>

  <!-- ios -->
  <platform name="ios">

    <config-file target="*-Info.plist" parent="CFBundleDisplayName">
      <string>Sample App</string>
    </config-file>

  </platform>

</plugin>